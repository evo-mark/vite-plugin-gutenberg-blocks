import { useBlockProps } from "@wordpress/block-editor";

export default function Save({ attributes }) {
    const blockProps = useBlockProps();

	return (
		<div {...blockProps}>
			// Saved block goes here
		</div>
	);
}
